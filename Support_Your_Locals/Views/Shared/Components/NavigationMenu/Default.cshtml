@model Tuple<string, IEnumerable<string>>

<a class="btn btn-block btn-outline-secondary" asp-action="Index" asp-controller="Home" asp-route-product="">Home</a>

@foreach (string product in Model.Item2) {
    <a class="btn btn-block @(product == Model.Item1 ? "btn-primary" : "btn-outline-secondary")"
       asp-action="Index" asp-controller="Home" asp-route-product="@product" asp-route-page="1">@product</a>
}
